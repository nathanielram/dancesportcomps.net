<p>
  <strong>Name:</strong>
  <%= @location.name %>
</p>

<p>
  <strong>Location:</strong>
  <%= @location.location %> <br /> <%= @location.location_short %>
</p>

<% if user_signed_in? && (can? :manage, @location) %>
<%= link_to 'Edit', edit_location_path(@location) %> |
<% end %>
<%= link_to 'Back', competitions_path %>